# Rust CLI binary (crates.io: dairo). No dev server — it's a command-line
# tool, not a servable app — so no dev/port/health.
name = dairo-cli
image = universal
build = cargo build --release --locked
test = cargo test --locked
artifacts = target/release
