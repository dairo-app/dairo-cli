name = "{{project_name}}"
main = "src/index.ts"
compatibility_date = "2024-09-23"

# DAIRO_API_KEY and DAIRO_WEBHOOK_SECRET are secrets, not plaintext vars.
# Set them with:
#   wrangler secret put DAIRO_API_KEY
#   wrangler secret put DAIRO_WEBHOOK_SECRET
# For local `wrangler dev`, put them in .dev.vars (see .dev.vars.example).
